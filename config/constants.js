module.exports = {
  PORT: 4000,
  DEFAULT_TIMER: 60,
  MAX_ATTEMPTS: 3,
  MIN_PLAYERS_TO_START: 3, // "more than two players"
  GAME_STATES: {
    WAITING: 'waiting',
    IN_PROGRESS: 'in_progress'
  }
};